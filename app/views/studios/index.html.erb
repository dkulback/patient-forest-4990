<div class = "studio">
<% @studios.each do |studio| %>
  <%= studio.name %>
  <%= studio.location %>
  <section class = "movies" >
    <% studio.movies.each do |movie| %>
      <%= movie.title %>
    <% end %>
  </section>
<% end %>
</div>
